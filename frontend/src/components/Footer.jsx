function Footer() {
  return (
    <footer className="mt-12 border-t border-slate-800/70 py-6">
      <div className="mx-auto flex w-full max-w-7xl flex-col items-center justify-between gap-2 px-4 text-sm text-slate-400 sm:flex-row sm:px-6">
        <p>Built for fast ecommerce category scraping workflows.</p>
        <p>React + Tailwind + Express + Puppeteer</p>
      </div>
    </footer>
  );
}

export default Footer;
